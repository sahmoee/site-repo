# AI instructions

Stocked's sources page documents formats and data rights, not third-party endorsement or
redistribution permission. Preserve Open Prices/Open Food Facts ODbL and OpenStreetMap attribution,
separate photo terms, publisher rights and honest app-version availability language.
Archive/migration acknowledgements distinguish independently implemented data-format support from
copied dependencies; never publish imported files, household notes or private watch-folder paths.
Connection acknowledgements must distinguish API clients from copied server code and reviewed
calendar publication from autonomous two-way sync. Keep external setup and app-version limits clear.

Before editing, read `README_FIRST.md`, then `CROSS_PROJECT_SYNC.md`. Read only relevant README/page/config sections afterward. Publish no secrets, private QA data, unconfirmed claims, or placeholder store links. Verify affected static routes/content. Update these guides when publishing or shared-content contracts change.

Every page, dialog, drawer, and overlay must use the owning product theme without unstyled white surfaces. Layout, typography, controls, and navigation must remain responsive to viewport size, orientation, zoom, safe-area insets, and accessibility preferences. Fixed dimensions are only for intentional media aspect ratios. Avoid overflow locks or scroll containers that trap, clip, or duplicate page scrolling.
